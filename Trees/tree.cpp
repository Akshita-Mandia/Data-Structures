#include<iostream>
using namespace std;
void insert(int * val,int * s,int * arr)
{
    int t=1;
    if(s==1)
    {
        arr[t]=val;
        t++;
    }
    else if(arr[s]>=)
}
 main()
 {
     int num,tree[55];
     for(i=1;i<=55;i++)
        tree[i]=-1;
     cout<<"enter num: and -1 to exit: ";
     cin>>num;
     while(num!=-1)
     {
         insert(&num,&s,&tree);
         cout<<"num: ";
         cin>>num;
     }
 }
